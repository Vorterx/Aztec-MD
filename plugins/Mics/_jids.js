const { Zenith } = require('../../lib/functions');

Zenith({
  usage: 'jid',
  desc: 'Mmh ldk but its useless though',
  category: 'Mics',
}, async (vorterx,m, react) => {

       await react('😋');
  await m.reply(m.chat);
}
       );
